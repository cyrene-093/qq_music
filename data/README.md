# data

本目录存放运行时数据，**不要提交到公开仓库**。

| 文件 | 说明 |
| --- | --- |
| `settings.json.example` | 空白配置示例（可提交） |
| `settings.json` | 运行时生成：全局设置、统计、点歌记录等（勿提交） |
| `group_remarks.json` | 运行时生成：群显示名、QQ 群号等（勿提交） |

真实路径通常为：`plugins/qq_music/data/`。
