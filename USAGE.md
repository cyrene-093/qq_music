# 使用说明

## 1. 安装

1. 将插件目录放到 `plugins/qq_music`（目录名必须是 `qq_music`）。
2. 重载插件或重启机器人。
3. 确认 Web 侧边栏出现 **QQ音乐**，群内可响应 `#音乐帮助`。

无额外 Python 依赖，使用框架自带 HTTP 客户端。

---

## 2. 基本用法

### 个人点歌

```text
#点歌 稻香
#听1
```

- `#点歌 歌名`：搜索，返回最多 10 条结果。
- `#听N`：播放第 N 首，例如 `#听1`、`#听3`。
- 仅搜索发起人可以 `#听N`。
- 列表约 **3 分钟**有效，过期需重新搜索。

### 群点歌

```text
#群点歌 海阔天空
#群听1
```

- `#群点歌 歌名`：生成本群共享列表。
- `#群听N`：群内任意成员可播放。
- 新的 `#群点歌` 会覆盖本群上一份列表。
- 与个人 `#点歌` / `#听N` 互不影响。

### 播放效果

- 优先发送语音消息；过长时自动改为文件。
- 播放消息含封面、歌手、专辑；开启歌词时附带 LRC。
- 支持全角前缀，例如 `＃点歌`。

---

## 3. 指令一览

| 指令 | 权限 | 说明 |
| --- | --- | --- |
| `#点歌 歌名` | 所有人 | 个人搜索 |
| `#听N` | 搜索发起人 | 播放个人列表第 N 首 |
| `#群点歌 歌名` | 群聊 | 本群共享搜索 |
| `#群听N` | 群成员 | 播放群列表第 N 首 |
| `#歌源` | 所有人 | 查看当前歌源与说明 |
| `#歌源 名称或序号` | 群主/管理员/机器人主人 | 切换歌源 |
| `#歌源 跟随` | 同上 | 本群改回跟随全局 |
| `#歌词开` / `#歌词关` / `#歌词开关` | 所有人 | 个人歌词开关 |
| `#群歌词开` / `#群歌词关` | 群主/管理员 | 本群歌词开关 |
| `#群歌词状态` | 所有人 | 查看本群歌词状态 |
| `#音乐帮助` | 所有人 | 帮助 |

私聊中 `#歌源` 修改的是**全局**歌源；群聊中修改的是**本群**覆盖（除非使用 `#歌源 跟随`）。

---

## 4. 歌源

### 如何选择

| 场景 | 建议 |
| --- | --- |
| 日常点歌 | 第三方（默认） |
| 搜索结果不准 | 官方搜索 |
| 想要更高音质 | 落月 QQ |
| 第三方 / 落月主域不可用 | 落月备用域 |
| QQ 曲库没有 / 想换平台 | 网易云 |

切换示例：

```text
#歌源
#歌源 1
#歌源 落月
#歌源 网易云
#歌源 跟随
```

换源后请重新 `#点歌` 或 `#群点歌`。已搜出的列表仍按**搜索当时**的歌源播放。

### 内置歌源

| 序号 | 名称 | ID | 说明 |
| --- | --- | --- | --- |
| 1 | QQ音乐（第三方） | `aa_qq` | 默认。搜播一体，依赖第三方可用性 |
| 2 | QQ音乐（官方搜索） | `official_qq` | 搜索直连 QQ；播放自动尝试其他接口 |
| 3 | QQ音乐（落月） | `vkeys_qq` | 常为较高音质；部分曲目可能试听 |
| 4 | QQ音乐（落月备用域） | `epdd_qq` | 落月镜像 |
| 5 | 网易云（落月） | `vkeys_netease` | 网易云曲库 |
| 6 | 网易云（落月备用域） | `epdd_netease` | 网易云镜像 |

也可用别名，例如：`第三方`、`官方`、`落月`、`网易`、`epdd`。

### 自定义接口

在 Web 面板「歌源选择」底部填写 URL，并保存。

要求：

- 搜索地址需能用关键词查询，建议包含 `{msg}`。
- 播放地址建议包含 `{msg}` 与 `{n}`（序号，从 1 开始）。
- 若只填搜索地址且不含 `{n}`，插件会自动按 `...&n={n}` 补全。

示例：

```text
https://example.com/qq.music?msg={msg}
https://example.com/qq.music?msg={msg}&n={n}
```

接口返回 JSON，播放结果中需能解析出音频地址字段（如 `music` / `url`）。

---

## 5. 歌词

优先级：**群显式关闭 > 个人设置 > 群设置 > 全局默认**。

- 个人「跟随」时，再看群设置与全局。
- 群关闭歌词后，普通成员无法用 `#歌词开` 强制打开。
- 全局开关可在 Web 面板顶栏修改。

---

## 6. Web 管理面板

入口：机器人 Web 控制台侧边栏 **QQ音乐**。

| 页签 | 内容 |
| --- | --- |
| 概览 | 统计、热门、趋势、歌源选择、自定义接口 |
| 点歌记录 | 按类型、群、用户、关键词筛选 |
| 用户设置 | 用户歌词偏好 |
| 群歌词控制 | 群歌词开关、群备注、群头像 |
| 关于 | 插件简介、指令、歌源说明、注意事项 |

顶栏：

- 全局歌源、全局歌词
- 日/夜主题
- 选择群后：本群歌源覆盖、本群歌词、查看该群记录

群头像：在「群歌词控制」填写**数字 QQ 群号**并保存（不是群 openid）。

修改面板后若界面异常，请重载插件并强制刷新浏览器（Ctrl+F5）。

---

## 7. 数据文件

运行后会在插件数据目录生成：

| 文件 | 内容 |
| --- | --- |
| `settings.json` | 全局设置、统计、点歌记录、用户/群配置 |
| `group_remarks.json` | 群显示名、QQ 群号等备注 |

开发目录下的 `data/settings.json.example` 为空白示例。

发布或上传仓库时：

- 不要提交含真实用户 openid、昵称的 `settings.json`
- 不要提交含隐私的运行日志

---

## 8. 常见问题

**搜不到歌**  
换关键词；确认机器人能访问当前歌源接口；可换其他歌源再试。

**提示列表过期**  
重新 `#点歌` / `#群点歌` 后再 `#听N`。

**能搜到但播不了**  
该源可能暂时无直链。切换到「第三方」或「落月」后重新搜索。

**只有很短一段音频**  
部分接口对部分曲目只返回试听。插件会尽量自动换源；仍不行时换默认第三方后重新点歌。

**官方搜索能搜但不能播**  
常见情况：官方播放直链通常不可用，插件会自动尝试其他接口。

**汽水音乐**  
暂不支持按歌名 `#点歌`（无稳定公开搜索接口）。

**Web 扩展页只显示 `页面不存在`**  
确认目录完整（含 `panel.html`）、插件已启用，日志有「面板已注册」，然后 Ctrl+F5。

**第三方源整体失败**  
`#歌源 落月` 或 `#歌源 落月备用`，或在面板配置自定义接口。

**别人点不了我搜的歌**  
个人列表只有发起人可听。全群一起听请用 `#群点歌` / `#群听N`。

**群头像不显示**  
填写数字 QQ 群号并保存，然后刷新面板。

---

## 9. 源码与目录说明

仓库根目录即插件目录（安装后路径为 `plugins/qq_music/`）。

| 路径 | 作用 |
| --- | --- |
| `main.py` | 插件入口。注册 `#点歌`、`#群点歌`、`#听N`、`#歌源`、歌词相关指令；处理消息回复；加载/卸载时注册 Web 面板 |
| `service.py` | 核心业务。搜索结果缓存、发起播放、拉取/解析 LRC、拼装搜索列表与播放详情文案、按钮定义 |
| `sources.py` | 歌源层。定义内置源与别名、解析 `#歌源` 参数、对各源执行搜索/播放，失败或试听时自动备用 |
| `official_qq.py` | QQ 官方搜索。直连 y.qq.com 相关接口 |
| `settings.py` | 数据存储。读写全局/群/用户设置、点歌统计、记录、群备注相关数据 |
| `web_panel.py` | Web API。给管理面板提供设置、歌源、统计、记录、用户/群管理接口 |
| `panel.html` | 管理面板页面。歌源选择、自定义接口、统计图表、记录筛选、用户与群控制 |
| `assets/bg/README.md` | 背景图说明 |
| `assets/bg/day.jpg` | 日间背景（可选，需自行放入） |
| `assets/bg/night.jpg` | 夜间背景（可选，需自行放入） |
| `data/README.md` | 运行时数据目录说明 |
| `data/settings.json.example` | 空白配置示例，供参考结构 |
| `data/settings.json` | **运行时生成**。真实配置与用户数据，发布时不要上传 |
| `data/group_remarks.json` | **运行时生成**。群显示名、QQ 群号等备注 |
| `LICENSE` | MIT 许可证全文 |
| `README.md` | 简介、安装、目录 |
| `USAGE.md` | 本使用说明 |
| `.gitignore` | 忽略运行时数据与缓存 |

---

## 10. 版本与声明

当前版本：**1.9.8**

音源可用性与音质取决于上游接口，不保证永久可用或全部歌曲完整可播。请遵守当地法律法规与版权要求，本插件仅供学习与交流。
