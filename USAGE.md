# QQ 音乐点歌 · 使用说明

## 安装

1. 将 **`qq_music`** 文件夹放入机器人 `plugins/` 目录（与 `main.py` 同级的那一层插件目录）。
2. 在 Web 控制台或命令行 **重载插件** / 重启机器人。
3. 侧边栏扩展页出现 **「QQ音乐」** 即安装成功。

无需额外 `pip` 依赖，使用框架内置 HTTP 客户端。

## 目录说明

| 路径 | 说明 |
| --- | --- |
| `main.py` | 指令入口、Web 面板注册 |
| `service.py` | 搜索、播放、歌词拉取 |
| `settings.py` | 全局/用户/群歌词开关与统计持久化 |
| `web_panel.py` | Web 扩展 API |
| `panel.html` | Web 管理面板页面 |
| `assets/bg/` | 面板日间/夜间背景图 |
| `data/settings.json` | **运行时数据**（统计、记录、用户/群设置），首次运行自动创建 |
| `data/group_remarks.json` | 群备注（QQ 群号、显示名），按需生成 |


## 聊天指令

| 指令 | 说明 |
| --- | --- |
| `#点歌 歌名` | 搜索 QQ 音乐，返回列表（**3 分钟内**可 `#听N`） |
| `#听1` / `#点歌 1` | 播放列表第 N 首（优先语音，过长发文件） |
| `#歌词` | 查看最近一次播放的完整歌词 |
| `#歌词开` / `#歌词关` / `#歌词开关` | 个人歌词显示开关 |
| `#群歌词开` / `#群歌词关` / `#群歌词状态` | 群歌词开关（群主/管理员） |
| `#音乐帮助` | 显示帮助 |

也支持全角 `#`（`＃点歌`）。

### 歌词开关优先级

**个人设置 > 群设置 > 全局默认**

- 个人设为「跟随」时，才看群与全局。
- Web 面板可批量查看、修改用户与群的生效状态。

## Web 管理面板

路径：侧边栏 **QQ音乐** 扩展页。

### 概览

- 搜索 / 播放 / 歌词发送 / 跳过次数
- 热门歌曲 TOP、趋势图
- 快捷搜索用户、顶栏群切换

### 点歌记录

- 按类型（搜索/播放）、群 openid、用户 openid、关键词筛选
- 分页浏览

### 用户设置

- 列表查看用户歌词偏好与点歌次数
- 快捷搜索用户并切换歌词开关

### 群歌词控制

- 群列表与歌词状态
- **群资料备注**：填写群名称、数字 QQ 群号后保存，可显示群头像
- 备注 key 为群 **openid**（`group_id`），不是 QQ 群号

### 界面

- 顶栏 **日/夜切换**：切换面板背景与配色
- 背景图位于 `assets/bg`，可自行替换（建议 1920×1080 JPG）

## 音源说明

- 搜索与播放：`https://a.aa.cab/qq.music`
- 歌词：QQ 音乐官方歌词接口

仅供学习交流，请遵守版权与平台规则。

## 数据与缓存

- 点歌记录、统计、用户/群设置保存在 **`data/settings.json`**（最多保留约 500 条记录）。
- 群备注保存在 **`data/group_remarks.json`**。
- 框架运行时会将数据写到插件数据目录；开发目录下的 `data/` 仅为兜底路径。
- 搜索列表在内存中缓存约 **3 分钟**，过期需重新 `#点歌`。

## 常见问题

**搜不到歌** → 换关键词或稍后再试；检查机器人出站网络能否访问音源 API。

**播放失败** → 列表过期请重新搜索；部分歌曲可能无可用音源。

**群头像不显示** → 在 Web「群歌词控制」填写 **数字 QQ 群号**（非 openid），保存后刷新。

**Web 面板空白** → 确认已重载插件；浏览器硬刷新（Ctrl+F5）。


## 版本

1.5.0
